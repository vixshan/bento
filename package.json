{
  "name": "bento-custom-domain",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "node ./app.js",
    "start": "pm2 start app.js --name bento && pm2 save && pm2 logs",
    "dev": "nodemon ./app.js",
    "u": "git pull && npm i && npm run f && npm run dev",
    "f": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^4.26.1",
    "newrelic": "^11.11.0",
    "node-fetch": "^3.3.2",
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
